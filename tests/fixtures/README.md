# 合成测试数据

本目录只保存为了离线测试手工构造的合成数据。姓名、岗位、公司、学校、候选 ID、岗位 ID、请求和响应字段均为虚构或占位内容，不来自真实候选人、真实 HR 账号或生产日志。

新增或修改 fixture 时不得粘贴真实简历、联系方式、Cookie、Token、认证头、账号标识、抓包原文或本地文件路径。HAR fixture 只能保留测试解析所需的最小请求 URL 和合成响应，headers 与 cookies 必须为空。
